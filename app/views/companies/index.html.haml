.accordion#accordion2
  - @industries.each do |industry|
    .accordion-group
      .accordion-heading
        %a.accordion-toggle(data-toggle="collapse" data-parent="#accordion2" href="companies/list/#{industry.id}" data-target="#industry_#{industry.id}" data-remote="true")
          =industry.name
      .accordion-body.collapse{:id => "#{industry.id}"}


- content_for :sidebar do
  -#TODO progress bar
  #show_profile
